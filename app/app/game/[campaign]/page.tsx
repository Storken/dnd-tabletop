export default function Page ({ params }: { params: { campaign: string } }) {
  return (
    <main>
      <strong>The DM</strong> will be able to move characters between scenarios
      and place monsters etc on boards
      <br />
      <br />
      <strong>Players</strong> will only see the scenario they are placed on
    </main>
  )
}
